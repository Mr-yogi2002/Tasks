numbers = 73

numbers = str(numbers)

first_digit =int(numbers[0])
last_digit =int(numbers[-1])

integer=first_digit+last_digit

print("addition first_digit and last_digit of the numbers",integer)